name: Release

# This workflow runs automatically when a version tag (v*) is pushed.
# The CI workflow's auto-tag job creates these tags when package.json version changes.
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  create-release:
    runs-on: ubuntu-latest
    needs: publish-npm
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            install.sh
            install.ps1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Optional: Build standalone binaries for curl install
  # Uncomment this section if you want to provide pre-built binaries
  #
  # build-binaries:
  #   strategy:
  #     matrix:
  #       include:
  #         - os: ubuntu-latest
  #           target: linux-x64
  #         - os: ubuntu-latest
  #           target: linux-arm64
  #         - os: macos-latest
  #           target: darwin-x64
  #         - os: macos-latest
  #           target: darwin-arm64
  #         - os: windows-latest
  #           target: windows-x64
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: oven-sh/setup-bun@v1
  #       with:
  #         bun-version: latest
  #
  #     - name: Install dependencies
  #       run: bun install
  #
  #     - name: Build binary
  #       run: bun build src/index.ts --compile --outfile magic-shell
  #
  #     - name: Package (Unix)
  #       if: matrix.os != 'windows-latest'
  #       run: |
  #         tar -czvf magic-shell-${{ github.ref_name }}-${{ matrix.target }}.tar.gz magic-shell
  #
  #     - name: Package (Windows)
  #       if: matrix.os == 'windows-latest'
  #       run: |
  #         Compress-Archive -Path magic-shell.exe -DestinationPath magic-shell-${{ github.ref_name }}-${{ matrix.target }}.zip
  #
  #     - name: Upload Release Assets
  #       uses: softprops/action-gh-release@v1
  #       with:
  #         files: |
  #           magic-shell-*.tar.gz
  #           magic-shell-*.zip
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
